<div class="form-group ">
  <input required type="text" name="title" id="title" class="input-form" value="<%= newArticle.title %>"
    placeholder="Enter your title here">
</div>
<div>
  <label for="img" class="img-upload"> <% if(type== 'newArticle'){ %>
    Upload a feature
    <% } else { %>
      Update the featured 
      <% } %>
    
    image :</label>
  <input type="file" id="img" name="img" accept="image/*"class="img-upload" <% if(type == 'newArticle' ) { %>
  required
  <% } %>
    >
</div>
<div class="form-group">
  <textarea required type="text" name="markdown" id="markdown" class="input-form markdown "
    placeholder="Write your article( in markdown )"><%= newArticle.markdown %></textarea>
</div>